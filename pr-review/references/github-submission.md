# GitHub review submission

Use after explicit publication authorization and an evidence-backed finding or closure recommendation.

Submit one `COMMENT` review. Include truthful automation disclosure, use the repository/PR language, put line-specific findings inline, and keep cross-cutting findings in the body.

Build a temporary JSON payload:

```json
{
  "body": "> Automated review generated by <TOOL>.\n\n<concise review body>",
  "event": "COMMENT",
  "comments": [
    {"path": "<file>", "line": 123, "side": "RIGHT", "body": "<finding>"}
  ]
}
```

Submit and retain the returned URL:

```bash
gh api repos/<OWNER>/<REPO>/pulls/<PR>/reviews --input <payload.json> --jq '{id,state,html_url}'
```

Use `line` + `side` from `changed_line.py`. For an anchor rejected by GitHub, derive legacy `position` from `gh pr diff` as a fallback. `REQUEST_CHANGES` requires authorization for that exact event; this workflow emits `COMMENT`.
